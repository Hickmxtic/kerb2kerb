# Dashboard (pending)

Source not yet migrated. This will hold the ops dashboard (bookings, debt/emergency fund tracker, income & expenses, idea generator) currently living as a Claude artifact.

Data is entirely client-side (`localStorage`) — no backend.
